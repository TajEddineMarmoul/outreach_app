# Image-generation prompt set

Method: built-in image-generation tool, one image per page, plus one targeted Analytics revision.

Reference for Campaigns: the current implemented campaign message screen, with Help removed from the header.
Reference for the other five pages: the generated Campaigns image.
Reference for the Analytics revision: its initial generated image.

## Campaigns

Output: 01-campaigns.png

```text
Use case: ui-mockup.
Edit the supplied Outreach screenshot into a polished companion page of the same web application. The reference is the app's established visual style: white canvas, dark navy text, restrained vivid blue #0751f8, thin pale gray borders, subtle green/amber status labels, simple line icons, readable sans-serif type. Preserve its OUTREACH wordmark and labeled hamburger Menu at upper left, and small circular TA avatar at upper right. Remove the current campaign title, All campaigns back link, Draft badge, save status, composer, and bottom campaign step navigation: this is a top-level application page, not a campaign-builder step.
Create ONE complete desktop screen, landscape 16:9, straight-on flat UI screenshot, edge-to-edge interface with no browser chrome, laptop device, decorative scene, perspective, collage, annotations or watermark. Use a slim 60px global header and a balanced centered main content area about 1120px wide, with compact 44px controls, readable type, and comfortable whitespace. All necessary content must fit in one ordinary laptop viewport. A quiet white background; no giant headings or oversized cards. Main navigation stays CLOSED in Menu. Help belongs inside that closed menu: NO visible question-mark help button. Exactly one blue primary action, and a small number of subtle secondary controls. Do not display every available feature. Hide advanced features, bulk actions, delete/export commands, import settings and detailed configuration inside closed dropdowns or three-dot menus. No sidebars. No colorful tile dashboards. Use exact clean readable English text and realistic sample data. These are design concepts; never show open-rate, click-tracking, TrackMailBox, Gmail draft shortcuts, required attachments, AI assistants or unimplemented tracking metrics.
PAGE SPECIFICATION:
Show the Campaigns list, designed for someone returning to their campaigns.
Heading "Campaigns", subtitle "Create, review, and manage your campaigns." Blue top-right button "+ New campaign".
Below it, one compact toolbar: Search campaigns input on left, closed "All statuses" dropdown on right. Main content is one clean full-width table with columns "Campaign", "Status", "Progress", "Updated", and an unlabeled narrow actions column.
Exactly four rows:
"Fall hiring outreach" | green "Running" | "62 of 128 sent" with very thin half-filled blue progress line | "2 min ago" | three dots.
"Product introductions" | amber "Draft" | "Not started" | "Today" | three dots.
"September follow-up" | muted blue "Scheduled" | "Starts Sep 2" | "Yesterday" | three dots.
"Summer introductions" | neutral "Completed" | "86 of 86 sent" | "Aug 25" | three dots.
Under table only quiet text "4 campaigns". Row names act like links but are not giant buttons. Do not expose pause/end/delete/duplicate buttons on each row. No summary KPI cards, no activity feed, no large onboarding panel. Generous calm blank space after the short useful table.
```

## Contacts

Output: 02-contacts.png

```text
Use case: ui-mockup.
Edit the supplied Outreach screenshot into a polished companion page of the same web application. The reference is the app's established visual style: white canvas, dark navy text, restrained vivid blue #0751f8, thin pale gray borders, subtle green/amber status labels, simple line icons, readable sans-serif type. Preserve its OUTREACH wordmark and labeled hamburger Menu at upper left, and small circular TA avatar at upper right. Remove the current campaign title, All campaigns back link, Draft badge, save status, composer, and bottom campaign step navigation: this is a top-level application page, not a campaign-builder step.
Create ONE complete desktop screen, landscape 16:9, straight-on flat UI screenshot, edge-to-edge interface with no browser chrome, laptop device, decorative scene, perspective, collage, annotations or watermark. Use a slim 60px global header and a balanced centered main content area about 1120px wide, with compact 44px controls, readable type, and comfortable whitespace. All necessary content must fit in one ordinary laptop viewport. A quiet white background; no giant headings or oversized cards. Main navigation stays CLOSED in Menu. Help belongs inside that closed menu: NO visible question-mark help button. Exactly one blue primary action, and a small number of subtle secondary controls. Do not display every available feature. Hide advanced features, bulk actions, delete/export commands, import settings and detailed configuration inside closed dropdowns or three-dot menus. No sidebars. No colorful tile dashboards. Use exact clean readable English text and realistic sample data. These are design concepts; never show open-rate, click-tracking, TrackMailBox, Gmail draft shortcuts, required attachments, AI assistants or unimplemented tracking metrics.
PAGE SPECIFICATION:
Show the saved Contacts library, with contact groups available through a dropdown rather than a permanent sidebar.
Heading "Contacts", subtitle "People saved for your outreach." Blue top-right "+ Add contacts" button with subtle dropdown chevron; its menu is closed. Adjacent small three-dot More button.
Below: search input "Search contacts" on left, one closed "All groups" dropdown next to it. Quiet "128 contacts" count at right.
One wide clean table with small unselected checkboxes in the first column, then "Name", "Email", "Company", "Status", and a very narrow three-dot row-actions column. Exactly four sample rows:
"Maya Chen" | "maya@acme.example" | "Acme Corp" | green "Ready"
"Daniel Ortiz" | "daniel@linear.example" | "Linear Labs" | green "Ready"
"Amir Haddad" | "amir@atlas.example" | "Atlas Systems" | green "Ready"
"Lina Brooks" | "lina@north.example" | "North Studio" | green "Ready".
Small pale initial avatars beside names. Thin row dividers, natural hierarchy, no bulky cards.
Under the table, "Showing 1–4 of 128" and simple small previous/next pagination controls aligned right. Do not open the add/import menu, do not show an import form, do not show a separate contact-details panel, do not show bulk-delete or do-not-contact forms. Those are discoverable in the closed controls. Preserve a calm approachable contact list with no clutter.
Match the shared header, type scale, alignment and compact proportions of the supplied Campaigns concept; replace its page content with the specification above.
```

## Senders

Output: 03-senders.png

```text
Use case: ui-mockup.
Edit the supplied Outreach screenshot into a polished companion page of the same web application. The reference is the app's established visual style: white canvas, dark navy text, restrained vivid blue #0751f8, thin pale gray borders, subtle green/amber status labels, simple line icons, readable sans-serif type. Preserve its OUTREACH wordmark and labeled hamburger Menu at upper left, and small circular TA avatar at upper right. Remove the current campaign title, All campaigns back link, Draft badge, save status, composer, and bottom campaign step navigation: this is a top-level application page, not a campaign-builder step.
Create ONE complete desktop screen, landscape 16:9, straight-on flat UI screenshot, edge-to-edge interface with no browser chrome, laptop device, decorative scene, perspective, collage, annotations or watermark. Use a slim 60px global header and a balanced centered main content area about 1120px wide, with compact 44px controls, readable type, and comfortable whitespace. All necessary content must fit in one ordinary laptop viewport. A quiet white background; no giant headings or oversized cards. Main navigation stays CLOSED in Menu. Help belongs inside that closed menu: NO visible question-mark help button. Exactly one blue primary action, and a small number of subtle secondary controls. Do not display every available feature. Hide advanced features, bulk actions, delete/export commands, import settings and detailed configuration inside closed dropdowns or three-dot menus. No sidebars. No colorful tile dashboards. Use exact clean readable English text and realistic sample data. These are design concepts; never show open-rate, click-tracking, TrackMailBox, Gmail draft shortcuts, required attachments, AI assistants or unimplemented tracking metrics.
PAGE SPECIFICATION:
Show the Senders page as a simple place to choose and maintain connected email accounts.
Heading "Senders", subtitle "Email accounts used to send your campaigns." Blue top-right "+ Connect account" button.
Below, a closed dropdown "Hiring team" with a small "2 connected accounts" count; place a small three-dot menu beside the group dropdown for group actions.
One compact clean table: "Account", "Status", "Daily limit", "Sent today", narrow three-dot actions.
Two rows: "Taj Dine" with smaller "taj@outreach.example" below | green dot "Connected" | "40 emails" | "12 of 40" with a small thin progress bar | three dots.
"Recruiting team" with smaller "hiring@outreach.example" below | green dot "Connected" | "30 emails" | "8 of 30" with a small thin progress bar | three dots.
Below table a single understated note "Limits help control how many emails each account sends."
Keep group creation, changing limits, reconnecting, removing accounts and technical OAuth details hidden in closed menus. No settings forms open, no credential fields, no giant provider cards, no always-visible delete icons, no two competing primary actions.
Match the shared header, type scale, alignment and compact proportions of the supplied Campaigns concept; replace its page content with the specification above.
```

## Templates

Output: 04-templates.png

```text
Use case: ui-mockup.
Edit the supplied Outreach screenshot into a polished companion page of the same web application. The reference is the app's established visual style: white canvas, dark navy text, restrained vivid blue #0751f8, thin pale gray borders, subtle green/amber status labels, simple line icons, readable sans-serif type. Preserve its OUTREACH wordmark and labeled hamburger Menu at upper left, and small circular TA avatar at upper right. Remove the current campaign title, All campaigns back link, Draft badge, save status, composer, and bottom campaign step navigation: this is a top-level application page, not a campaign-builder step.
Create ONE complete desktop screen, landscape 16:9, straight-on flat UI screenshot, edge-to-edge interface with no browser chrome, laptop device, decorative scene, perspective, collage, annotations or watermark. Use a slim 60px global header and a balanced centered main content area about 1120px wide, with compact 44px controls, readable type, and comfortable whitespace. All necessary content must fit in one ordinary laptop viewport. A quiet white background; no giant headings or oversized cards. Main navigation stays CLOSED in Menu. Help belongs inside that closed menu: NO visible question-mark help button. Exactly one blue primary action, and a small number of subtle secondary controls. Do not display every available feature. Hide advanced features, bulk actions, delete/export commands, import settings and detailed configuration inside closed dropdowns or three-dot menus. No sidebars. No colorful tile dashboards. Use exact clean readable English text and realistic sample data. These are design concepts; never show open-rate, click-tracking, TrackMailBox, Gmail draft shortcuts, required attachments, AI assistants or unimplemented tracking metrics.
PAGE SPECIFICATION:
Show the message Templates library as a minimal list, not a grid of expanded email bodies.
Heading "Templates", subtitle "Reusable starting points for your emails." Blue top-right "+ New template" button.
Below, a search input "Search templates". Under it one clean list/table with columns "Template", "Subject", "Updated", and a narrow three-dot action column.
Three rows:
"Warm introduction" | "A quick question for {{company}}" | "Today" | three dots.
"Hiring outreach" | "Helping your team grow" | "Yesterday" | three dots.
"Simple follow-up" | "Following up on my note" | "Aug 27" | three dots.
Each template name has a small understated document outline icon. Rows tall enough to click comfortably but compact. Quiet footer count "3 templates".
Do not expose full email text, editing forms, preview pane, variable chips, attachment controls, or a second Create button. The user selects a template to open it; editing and preview are on demand. Plenty of white space, no card-heavy marketplace styling or categories that the product does not have.
Match the shared header, type scale, alignment and compact proportions of the supplied Campaigns concept; replace its page content with the specification above.
```

## Analytics

Output: 05-analytics.png

```text
Use case: ui-mockup.
Edit the supplied Outreach screenshot into a polished companion page of the same web application. The reference is the app's established visual style: white canvas, dark navy text, restrained vivid blue #0751f8, thin pale gray borders, subtle green/amber status labels, simple line icons, readable sans-serif type. Preserve its OUTREACH wordmark and labeled hamburger Menu at upper left, and small circular TA avatar at upper right. Remove the current campaign title, All campaigns back link, Draft badge, save status, composer, and bottom campaign step navigation: this is a top-level application page, not a campaign-builder step.
Create ONE complete desktop screen, landscape 16:9, straight-on flat UI screenshot, edge-to-edge interface with no browser chrome, laptop device, decorative scene, perspective, collage, annotations or watermark. Use a slim 60px global header and a balanced centered main content area about 1120px wide, with compact 44px controls, readable type, and comfortable whitespace. All necessary content must fit in one ordinary laptop viewport. A quiet white background; no giant headings or oversized cards. Main navigation stays CLOSED in Menu. Help belongs inside that closed menu: NO visible question-mark help button. Exactly one blue primary action, and a small number of subtle secondary controls. Do not display every available feature. Hide advanced features, bulk actions, delete/export commands, import settings and detailed configuration inside closed dropdowns or three-dot menus. No sidebars. No colorful tile dashboards. Use exact clean readable English text and realistic sample data. These are design concepts; never show open-rate, click-tracking, TrackMailBox, Gmail draft shortcuts, required attachments, AI assistants or unimplemented tracking metrics.
PAGE SPECIFICATION:
Show a restrained Analytics page focused ONLY on known email delivery results, without overwhelming new users.
Heading "Analytics", subtitle "See how your email delivery is performing." No blue main action is required on this informational page; use a single small outlined "Export report" action in a CLOSED three-dot menu instead of displaying it. Top-right controls: closed "Last 7 days" dropdown and a compact three-dot More button.
Below title, one quiet horizontal summary strip, not separate giant KPI cards: "128 Attempts" | "124 Sent" in understated green | "4 Failed" in understated red.
Below the strip, one clean large but modest chart titled "Emails sent" using seven slim blue vertical bars labeled Mon Tue Wed Thu Fri Sat Sun, heights corresponding to 14, 22, 19, 28, 24, 10, 7; small baseline and quiet y-axis. No gradient, no decorative graphics, no extra charts.
Under chart, a collapsed row "View delivery history" with chevron. Do not show a giant log table at the same time as the chart. No opens, replies, clicks, conversions, bounce-rate assumptions, or tracker-related metrics. All data are sample delivery counts and the chart's bars total 124. Fit the entire page in one laptop screen.
Match the shared header, type scale, alignment and compact proportions of the supplied Campaigns concept; replace its page content with the specification above.
```

## Settings

Output: 06-settings.png

```text
Use case: ui-mockup.
Edit the supplied Outreach screenshot into a polished companion page of the same web application. The reference is the app's established visual style: white canvas, dark navy text, restrained vivid blue #0751f8, thin pale gray borders, subtle green/amber status labels, simple line icons, readable sans-serif type. Preserve its OUTREACH wordmark and labeled hamburger Menu at upper left, and small circular TA avatar at upper right. Remove the current campaign title, All campaigns back link, Draft badge, save status, composer, and bottom campaign step navigation: this is a top-level application page, not a campaign-builder step.
Create ONE complete desktop screen, landscape 16:9, straight-on flat UI screenshot, edge-to-edge interface with no browser chrome, laptop device, decorative scene, perspective, collage, annotations or watermark. Use a slim 60px global header and a balanced centered main content area about 1120px wide, with compact 44px controls, readable type, and comfortable whitespace. All necessary content must fit in one ordinary laptop viewport. A quiet white background; no giant headings or oversized cards. Main navigation stays CLOSED in Menu. Help belongs inside that closed menu: NO visible question-mark help button. Exactly one blue primary action, and a small number of subtle secondary controls. Do not display every available feature. Hide advanced features, bulk actions, delete/export commands, import settings and detailed configuration inside closed dropdowns or three-dot menus. No sidebars. No colorful tile dashboards. Use exact clean readable English text and realistic sample data. These are design concepts; never show open-rate, click-tracking, TrackMailBox, Gmail draft shortcuts, required attachments, AI assistants or unimplemented tracking metrics.
PAGE SPECIFICATION:
Show Settings as a calm progressive-disclosure page with only basic general settings visible.
Heading "Settings", subtitle "Manage your workspace preferences."
Use a narrow centered form section with a quiet heading "General". Show exactly one clearly labeled editable setting: "Default timezone", a closed dropdown showing "Africa/Casablanca". Short helper text below: "Used when you create a new campaign. Each campaign can have its own timezone."
Immediately below the field, a modest blue primary button "Save changes".
Below the general section, two thin full-width collapsed disclosure rows, "Connected services" and "Advanced settings", each with a rightward chevron and no content expanded.
At the bottom of this small area show a subdued text link "Manage sender accounts" with a tiny right arrow.
Never show API keys, secrets, SMTP fields, OAuth tokens, tracking toggles, require-attachment options, feature flags, debug settings, destructive actions, password fields, numerous categories or extra cards. Do not expand any secondary settings. This screen should feel unusually quiet and usable, not like a control panel.
Match the shared header, type scale, alignment and compact proportions of the supplied Campaigns concept; replace its page content with the specification above.
```

## Analytics — final targeted revision

The first Analytics image incorrectly showed an open export menu. This final edit closed it while preserving the screen.

```text
Use case: precise-object-edit. Edit only this Analytics UI screenshot. Close the More menu: remove the floating white 'Export report' popup and its shadow located directly below the upper-right three-dot button. Restore the clean white background in precisely that popup area. Keep the three-dot button itself visible. Keep the Last 7 days selector, all header elements, all chart bars, labels, numbers, the delivery history disclosure, exact typography, spacing, colors and overall canvas unchanged. Every dropdown/menu must be closed in the finished screenshot. Do not move or redesign anything else.
```

